# plans/independent — parallel-safe work

One file per work item that has **no unmet dependency** — each can be handed to a separate agent
**right now** without waiting on anything. Each file lists the item's track, effort, any deps
(all already done), and the source plan. See ../AGENTS-INDEX.md for the full picture and the
dependent (blocked) items.

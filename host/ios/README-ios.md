# Canopy iOS Host

The build & structure documentation lives in **[README.md](./README.md)** (canonical).

This `README-ios.md` is the name referenced by the SHARED CONTRACT §1 file layout; it is kept as
a pointer so both names resolve to the same doc.
